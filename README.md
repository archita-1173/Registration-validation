# Registration-validation
Project is a part of HICH intern. Where the frontend sends registration to backend through REST apis ,it is then stored in DB, which is then verified by OPEN AI apis and verification status is renewed.
